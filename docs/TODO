> Error message when numbers don't add up - fix bug where text is not centered
> Add validation feedback for no matching names
> Add refresh button that replaces the Calculate button
> Split code into different files - need to decide/research options
> Warnings in console + NaN thing
> Auto fill issue
> horizontal scroll bar issues: padding at top and transaction rows
> Check Mobile
